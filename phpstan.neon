parameters:
    level: 8
    typeAliases:
        ContextData: '''
            array{
                project_name?: string,
                root_domain?: string,
                registry?: string,
                repository?: string,
                user_id?: int,
                build_args?: string[],
                twig_dockerfile_frontend?: string,
                docker_profiles?: string[],
                resolve_domains_via_host?: bool,
                router_autostart?: bool,
                worktree?: ?string,
                worktree_isolation?: bool,
                worktree_directory?: string
            }
        '''
    paths:
        - src

includes:
    - phpstan-baseline.neon
