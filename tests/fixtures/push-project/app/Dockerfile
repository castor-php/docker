# Something to build that is not a PHP image: this fixture is about what
# docker:push pushes, not about what the plugin builds.
FROM alpine:3.22

ARG greeting=hello
RUN echo "${greeting}" > /greeting
